class Message:
    # TODO: same approach as in mobile app where message is a byte number followed by raw data size and raw data itself
    REQUEST_SETTINGS = "REQUEST_SETTINGS"
    CURRENT_SPEED = "CURRENT_SPEED"
